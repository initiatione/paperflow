from __future__ import annotations


WIKI_RESET_CONFIRMATION = "确认重置 EPI wiki"
CONFIG_RESET_CONFIRMATION = "确认同时重置 EPI config"
CONFIG_RESTORE_CONFIRMATION = "确认恢复 EPI config"

PROTECTED_META_NAMES = {
    "epi-config.yaml",
    "epi-config-state.json",
    "config-history",
    "epi-config-history",
}
