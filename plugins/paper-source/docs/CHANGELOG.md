# Paper Source/Paper Wiki Changelog

## Unreleased

- Paper Source 2.9.0: add read-only discovery-time Zotero dedupe to Paper Source dry-run/discover evidence. Paper Source still checks wiki `_meta/reference-index.json` first, then uses the plugin-local official Zotero helper adapter only when Zotero is enabled/configured to classify candidates as `already_in_zotero_not_wiki`, `possible_zotero_duplicate`, `new_candidate`, `zotero_unavailable`, or `not_checked`. Runs now persist `zotero-dedupe-record.json` with schema `paper-source-zotero-dedupe-v1`, expose `report.json.discovery_context.zotero_dedupe`, and render `session_recommendations.zotero_dedupe` / report tables for Zotero-only and possible duplicate candidates. The path is read-only: no Zotero imports, no formal Paper Wiki pages, no `_meta/reference-index.json` writes, no `references.bib`, and no Paper Wiki sync reports. Existing `_paper_source/raw/<slug>/zotero-record.json` sidecars remain record-only compatibility artifacts. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Wiki 1.3.0: add the approved Paper Wiki Zotero apply surface. Paper Wiki can now turn a reviewed dry-run plan into auditable writes only after one-run plan-hash approval: link existing Zotero items, import wiki-only DOI/arXiv-identified papers through the official helper, require the selected Zotero target to be the fixed `Paper Wiki` collection, update nested top-frontmatter `zotero:` metadata, supplement/validate missing bibliographic identity through injectable providers, snapshot only pages with non-empty bibliographic overwrites, refresh `_meta/reference-index.json`, write wiki-scoped `references.bib`, and record `_meta/zotero-sync/runs/<run-id>.json` plus `_meta/zotero-sync/index.json`. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Wiki 1.2.0: add the Paper Wiki Zotero status/dry-run surface. Paper Wiki now has a read-only planner and renderer for formal `references/*.md` Zotero health, validated link-existing plans, `ready_for_import` / `needs_metadata_supplementation` / conflict outcomes, selected-target apply warnings, and Zotero-only whole-library summary-plus-top-20 reporting. The workflow writes no formal pages, `_meta/reference-index.json`, `references.bib`, Zotero items, Paper Source sidecars, or sync report files by default; persisted diagnostics require an explicit output request. Apply/import orchestration remains a later task. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Wiki 1.1.0: add the Paper Wiki Zotero foundation layer. Paper Wiki now has plugin-local helpers for nested top-frontmatter `zotero:` metadata, optional compatible `_meta/reference-index.json` Zotero objects under the existing `paper-research-reference-index-v1` schema, `paper-wiki-zotero-sync-report-v1` report construction/validation, and wiki-scoped `references.bib` composition from item-level BibTeX records. This foundation does not call Zotero Desktop, does not import items, does not implement dry-run/status/apply orchestration, and does not change Paper Source discovery. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.8.2 / Paper Wiki 1.0.6: add plugin-local official Zotero helper adapters for PaperFlow. The adapters discover only trusted Zotero plugin/helper locations or explicit overrides, validate official helper identity and command surface, normalize helper subprocess failures into stable gates, redact secret-bearing diagnostics, and provide fake-helper tests for downstream Paper Source dedupe and Paper Wiki sync tasks. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.8.1 / Paper Wiki 1.0.5: harden Obsidian relationship graph maintenance after a live vault graph collapsed to only `index`. Paper Source `wiki-setup` now keeps `.obsidian/graph.json` global `search` empty and uses `.obsidian/app.json` `userIgnoreFilters` for `_paper_source/`, legacy `_epi/`, `_meta/`, `.claude/`, `AGENTS.md`, `hot.md`, and `log.md` instead of writing fragile formal-root graph search regexes. Paper Wiki routing and workflows now treat "关系图谱出错" / graph-only-index reports as first-class graph visibility repair: check display config, scan visible formal Markdown nodes/edges and broken wikilinks separately, repair display drift before link repair, and tell users to reopen the Graph tab or reload Obsidian when files are healthy but the live UI is stale. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.8.0: add `health-doctor` as the PaperFlow-wide diagnostic skill for Paper Source, Paper Wiki, MCP, runtime.json/env files, OpenAI-compatible/Grok gateway failures, Cloudflare/non-JSON upstream responses, MinerU, EasyScholar, Zotero, and vault contract health. The skill keeps a thin entrypoint with layered references and bundled redacted probes for env status, line-delimited stdio MCP initialize/tools-list checks, and metadata-only OpenAI-compatible `/v1/models` checks; repairs route back to the owning focused skills and remain read-only first. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.7.2: align discovery docs and the standalone query-planner helper with the canonical review/survey policy. Default `discover-papers` and helper query plans keep review/survey/meta candidates unless the request has explicit non-review intent; only then do query variants add `-review -survey` and filtering enforces document-type exclusions. Config init/propose/apply now return `unknown_keys` for ignored top-level payload typos and YAML single-quoted scalars unescape doubled quotes. Discovery cleanup hardens stable-identity rejection for completely unanchored candidates, preserves richer merged metadata when sparse paper-search records arrive later, narrows lexical suffix matching so overbroad forms such as `learningly` do not match, reports optional concept groups with separate `matched` and `passed`, and adds deterministic rank tie-breaking. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.7.1: fix P0/P1 discovery correctness issues in the source checkout. Grok MCP cleanup now reaps killed child processes and Grok model fallback / retry loops share one per-call timeout budget, reporting `timeout_or_budget_cutoff` with `timeout_budget_exhausted` instead of multiplying the configured timeout by fallback count. Research Brief required task/problem terms are preserved even when absent from the topic string, while ordinary config positive keywords remain ranking/soft evidence unless they match the current topic. Query planning now preserves CJK topic runs such as `强化学习` without adding an external tokenizer. Paper-search year parsing now uses a strict leading 4-digit 19xx/20xx prefix without raising on values like `2023-05`, `2023a`, or `forthcoming`, and tolerant int helpers treat bool values as absent. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.7.0: add runtime progress telemetry for long Paper Source discovery runs. `dry-run` now writes `progress-events.jsonl` and `progress-summary.json` using `paper-source-progress-events-v1`, emits concise phase start/end/heartbeat lines to stderr, and mirrors the latest summary into `run-state.json.discovery_progress` plus `report.json.discovery_context.discovery_progress`. Paper-search, Grok supplemental search, DOI recovery, normalization, filtering/recall-risk, quality enrichment, ranking, and report generation now expose elapsed time, counts when available, last-known phase, and artifact paths. `dry-run --json` and high-level artifact maps include `progress_events` / `progress_summary` paths without corrupting stdout JSON. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.6.0: add field-agnostic required concept groups for discovery relevance. Query plans can now carry `paper-source-required-concept-groups-v1` from explicit agent plans, Research Briefs, or conservative config/user-request derivation when a confirmed hard target anchor and a current-request task/problem term are both present. `filter_candidates_with_report` evaluates each group with the shared token-boundary lexical matcher, writes per-candidate `required_concept_groups`, `required_concept_groups_passed`, and `required_concept_group_failures`, rejects missing required groups with `required_concept_group_mismatch:<group_id>`, and summarizes group pass/failure counts in `filter-report.json`, `discovery-diagnostics.json`, and `report.json.discovery_context`. `session_recommendations.no_primary_recommendations_summary` now counts filter-level required-group failures as diagnostics instead of silently reporting a generic empty primary result. CLI/domain hard anchors remain domain gates unless an agent explicitly supplies required groups, so existing one-concept and soft-recall requests keep backward-compatible recall behavior. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.5.3: expose optional `grok-search-rs` through plugin MCP self-registration alongside `paper-search-mcp`. The new `grok-search-rs` outer launcher uses `cwd: "."` plus `cmd /c .\scripts\grok_search_mcp_launcher.cmd`, loads user-level `runtime.json` and `grok_search_mcp.env_file` / `env_files`, and starts the configured Grok stdio MCP without putting endpoint, token, model, or proxy details into `.mcp.json`. Paper Source discovery still uses the existing Grok adapter for targeted/parallel gating, model fallback, bounded retry, quality diagnostics, quarantine/evidence handling, and separate `grok-search-*` artifacts. `doctor --json` now validates both installed MCP launchers and warns when user-level `config.toml` statically shadows either `paper-search-mcp` or `grok-search-rs`. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.5.2: make configured Grok supplemental discovery retry-aware and diagnostic when a no-contribution run needs investigation. Grok remains a valuable optional supplemental recall source, not a paper-search replacement. `grok-search-record.json`, `grok-search-raw.json`, merged `search-record.json`, `provider-records.json`, `discovery-diagnostics.json`, and `report.md` now expose `paper-source-grok-search-diagnostics-v1` / `paper-source-grok-contribution-v1` fields for returned, usable, evidence-only, quarantined, merged, filtered, ranked, and accepted counts; failure stages distinguish provider/runtime failure, timeout/budget cutoff, source fallback, parser/normalization failure, page chrome, weak identity, and filter/rank rejection. Retryable source fallback/provider/runtime/parser failures record bounded retry attempts with safe query/domain/model metadata, elapsed time, and recovered/no-recovery/error outcome. Empty-title, page-chrome, and regex-only DOI/arXiv Grok records are evidence/quarantine by default unless a stable explicit paper identity is present. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.5.1: tighten the chat-ready discovery output contract after a live Paper Source session showed run artifacts containing existing-library and no-primary information that the final chat response omitted. `paper-discovery` output docs now require final responses to carry explicit sections for run status/artifact path, primary recommendations or `主推荐：无`, no-primary reasons and next action, review appendix, already-in-library/wiki reminders, and evidence/runtime caveats. Primary recommendations must render as a numbered list with paper title, venue/year plus DOI/citation evidence, and a required `summary：` line that extracts method, target problem, and reported effect/evidence from source abstracts; already-in-library/wiki papers remain a separate Markdown table. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.5.0: add local fixture discovery benchmark gates. New `discovery-benchmark` CLI reads `paper-source-discovery-benchmark-cases-v1` case files, reuses query-plan, normalize/filter, recall/risk, ranking, and session recommendation helpers, and emits `paper-source-benchmark-v1` output for `evaluation-brief` / Plugin Eval evidence. Benchmark cases can fail on precision, recall, review leakage, duplicate rate, verified metric coverage, normalized citation ordering, absolute citation display, and config-driven query-plan regressions. Core benchmark execution requires no secrets or live MCP availability. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.4.0: add provider-compatible recall-gap recovery and quality-risk checks to discovery ranking. Dry-run now writes `recall-gap-record.json` / `paper-source-recall-gap-record-v1` when first-pass kept candidates expose provider metadata for official journal versions, related papers, cited-by papers, or references; recovered candidates are normalized, filtered with the same hard constraints, and marked with `paper-source-recall-expansion-v1` evidence before ranking. Dry-run also writes `quality-risk-record.json` / `paper-source-quality-risk-record-v1`; ranked candidates carry `quality_risk` with `verified`, `suspected`, or `unverified` status, and missing risk data is explicitly `unverified` rather than treated as safe. Verified severe provider evidence for retraction, withdrawal, expression-of-concern, paper-mill, or predatory-venue risk lowers ranking eligibility through `quality_gate.dimensions.quality_risk`, `verified_quality_risk` blockers, and session recommendation cautions. Weak or missing risk evidence produces warnings only. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.3.14: improve query-planning relevance diagnostics and deterministic lexical matching. `query-plan.json` now includes `term_provenance_detail` and `paper-source-query-plan-diagnostics-v1` so agents can see config, user request, Research Brief, and agent-term sources plus warnings for raw or underspecified complex natural-language plans. Agent query plans can provide field-agnostic `synonyms`, `synonym_terms`, `acronym_expansions`, `acronyms`, `related_terms`, and `expanded_terms`; scripts record these as soft recall unless the agent separately supplies explicit hard anchors. Filtering and ranking now use token-boundary lexical matching with simple singular/plural handling instead of raw substring checks, avoiding false positives such as short acronyms matching inside unrelated words while preserving hard-anchor and negative-keyword gates. Grok remains supplemental and final recommendations still require the existing paper-search anchor policy. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.3.13: improve discovery ranking signal calibration. `rank_candidates` now treats configured venue priors as a real positive signal without letting venue reputation create Tier A alone; citation ranking uses publication-age-normalized citation quality while preserving raw absolute `citation_count` and citation provenance in reports/session recommendations; default freshness is relative to the current year or explicit `year_min`, not a hard-coded year. Method rigor, validation strength, benchmark evidence, reproducibility, and code/data availability now affect score, tier, and ranking rationale. Quality-evidence lexicons are additive and field-agnostic: Paper Source config, Research Briefs, and agent query plans can supply benchmark, reproducibility, and paper-type terms that are unioned with the defaults, so non-CS rigorous papers are not penalized purely for lacking CS/AI benchmark vocabulary. Missing citation/venue evidence is surfaced as missing or unverified in `ranking_signals` instead of being collapsed into verified zero evidence. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.3.12 / Paper Wiki 1.0.4: make the formal page frontmatter canonical/mirror boundary executable. Paper Wiki `rules/wiki-writing-standard.md` remains the canonical human-readable contract; `rules/formal-page-frontmatter.md` and Paper Source docs/skill/code constants now explicitly describe repeated field lists as focused or validation mirrors. Contract tests parse the canonical Paper Wiki required field list and compare it with the focused rule plus Paper Source `FORMAL_FRONTMATTER_REQUIRED_FIELDS`, so unexpected drift fails in source checkout. Formal page validation behavior is unchanged. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.3.11 / Paper Wiki 1.0.3: clarify PS/PW public entrypoints at the wording/metadata level. Paper Source `skills/wiki-setup/agents/openai.yaml` short description and default prompt drop the awkward "an Paper Source paper wiki vault" compound (grammar + clarity fix); `stage_wiki_brief.py` `provenance_policy` switches to policy wording ("preserve provenance labels and maintain tag/alias taxonomy") instead of the imperative "use wiki-provenance and tag-taxonomy together". Paper Wiki `docs/structure.md` distinguishes "no orchestrator-style user CLI" from the narrow helper wrapper scripts (figure maintenance, reference-index refresh, formal-page-contract migration). No skill is removed; ordinary Paper Wiki requests still route through `$paper-research-wiki`, `paper-wiki-language` remains a supporting language gate, and `wiki-ask`/`wiki-query` remain programmatic fallback/diagnostic surfaces. Discovery, source-staging, formal-page, and record semantics are unchanged. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.3.10: add script-size governance for Paper Source plugin Python scripts and split dry-run/search orchestration out of the oversized `orchestrator.py`. `orchestrator.py` now stays as the compatibility CLI/workflow surface while focused `orchestrator_dry_run.py` and `orchestrator_common.py` own dry-run/search flow and shared run lifecycle helpers. The Paper Source skill bundle contract now fails when any Python script under `plugins/paper-source/scripts` exceeds 2000 lines. Ranking, query-planning, DOI, staging, and Paper Wiki handoff semantics are unchanged. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.3.9: finish the `promote-to-wiki` retirement across active surfaces and give `discover-to-handoff` a single route owner. Removes the retired `promote-to-wiki`/`promote-after-approval` readiness and recommended-action branches from `run_index.py` (including the `_promote_action_command` and `_paper_gate_allows_promotion` helpers), the `promote-to-wiki` entry from the batch awaiting-promotion recognition in `orchestrator.py`, the legacy stage line in `routing-rules.example.yaml`, and the `promote-to-wiki` fallback wording from the quality-gates metric pack and its test fixture. `discover-to-handoff` now triggers only the `paper_ingest` route instead of duplicating across `paper_discovery` and `paper_ingest`, with a routing-contract test guarding the single owner. Recommended next actions now recognize only the agent-mediated `wiki-ingest-handoff` -> `record-human-approval` -> `wiki-ingest-trigger` -> `record-wiki-ingest` flow; historical changelog mentions of the retired command remain as history. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.3.8: make discovery ranking gates cross-discipline instead of tuned to one task/domain. `rank_candidates` now separates strict `priority_keywords` topic fit from long-profile positive keyword matching; when no hard anchors are supplied, positive keyword topic fit saturates after a small number of strong matches and preserves `keyword_coverage_score` only as diagnostics, so long multi-discipline profiles do not dilute a clearly relevant paper into `weak_topic_fit`. `quality_gate.dimensions` now exposes identity, relevance, inspectability, validation, source confidence, reproducibility, and request risk for every ranked candidate. Docs now clarify the `openags/paper-search-mcp` boundary: it is a broad retrieval, metadata, dedupe, download, and source-coverage provider, not a semantic quality gate or recommendation ranker; Paper Source owns query planning, source routing, quality gates, DOI policy, and chat-facing recommendation surfaces. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.3.7: calibrate discovery quality gates and recommendation status taxonomy after live AUV evidence showed clearly in-domain DOI/PDF papers being marked `quality_tier=Reject` only because broad query/profile keywords diluted hard-anchor topic fit. Query-plan priority ranking now uses hard domain anchors/domain focus terms instead of method/problem/context filler as the topic-fit denominator, while those soft terms remain ranking evidence. `session_recommendations.review_appendix` is now strictly non-Reject; quality Reject records move to `quality_reject_debug` diagnostics with blocking reasons and query/source provenance. Zero-primary runs now expose `no_primary_recommendations_summary` in `report.json` and `report.md` so agents can explain whether DOI policy, quality gates, existing-library saturation, or recall gaps caused an empty primary list. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.3.6: tighten the chat-facing existing-library format after AUV discovery feedback and make `_meta/reference-index.json` the canonical dedupe backlog when available. `paper-discovery` output docs now require `库中已有，可回看` to be rendered as a short explanatory sentence plus a Markdown table with paper/year/status/entry/DOI columns, instead of a one-line semicolon-separated list. `report.md` now mirrors this table shape under `Already In Library Or Wiki`, preserving the boundary that already-in-wiki/raw-library hits are reminders, not new recommendations. `paper_library.py` now skips raw metadata scans when wiki `_meta/reference-index.json` loads successfully and reports `raw_scan_policy`; `_paper_source/raw/*/metadata.json` is only a missing/unreadable-index fallback. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.3.5: require DOI-present chat-facing discovery recommendations without silently dropping promising papers. `normalize_candidates` now enriches DOI from provider DOI fields, DOI URLs, raw metadata, and deterministic arXiv DOI `10.48550/arXiv.<base_id>`, extracts public repository URLs from provider abstracts/snippets, and records enrichment provenance. Dry-run can run targeted Grok/web DOI recovery for remaining DOI-missing candidates, writing `doi-recovery-grok-*` artifacts before re-normalizing. `session_recommendations.primary_recommendations` and `review_appendix` now exclude remaining DOI-missing candidates and report recovery success/failure through `doi_recovery_summary`, `doi_resolution_summary`, and `doi_filtered_summary` / `missing_required_doi`; output docs now require the agent to keep primary recommendations, `新增待复核候选`, and `库中已有，可回看` separate and to report Grok as valid evidence only when the provider status is truly `ok` with usable records and no fallback warnings. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.3.4: make optional Grok supplemental discovery more resilient for configured OpenAI-compatible runtimes. The adapter now preserves the user's primary `OPENAI_COMPATIBLE_MODEL`, retries configurable/default multi-agent-first model fallbacks for `grok_provider_error` or `source_fallback` MCP responses, records non-secret `model_attempts` in `grok-search-raw.json`, and makes parallel Grok fan-in grace configurable through `PAPER_SOURCE_GROK_PARALLEL_GRACE_SECONDS` with a 180 second default. Docs now clarify that each user must configure their own Grok relay endpoint/key/model and that local proxy, TUN/fake-IP, DNS, or Cloudflare-challenge symptoms are runtime diagnostics, not plugin defaults. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.3.3: harden existing discovery contracts after live AUV-topic regressions. Grok supplemental search now uses the installed provider's supported detailed response format instead of unsupported JSON mode; ranking gives explicit query-plan hard anchors/task terms a priority topic-fit channel so narrow exact-topic papers are not diluted by long research profiles; normalized candidates preserve citation-count source provenance and no longer treat missing provider citation fields as verified zero counts; `session_recommendations` now exposes DOI/primary URLs, citation evidence fields, verified metrics, `verification_summary`, per-item verification warnings, and an `existing_library_appendix` for already-in-wiki/library hits. Existing papers remain excluded from new recommendations and auto-staging; they are only surfaced as a separate reminder section. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.3.2 follow-up: accept `GROK_SEARCH_API_KEY` as an optional Grok/search provider env-file alias while preserving OpenAI-compatible URL/model/key configuration as sufficient for `grok-search-rs`.
- Paper Source 2.3.2 follow-up: keep Unpaywall out of broad no-DOI first-pass keyword discovery. Default `paper_search.sources`, query-plan T1 routes, skill workflow examples, and source-tier docs now use arxiv/semantic/openalex/crossref for broad discovery. `plan_source_routing` demotes `search=doi-lookup` sources with `doi_lookup_source` unless the request is an exact DOI lookup, where Unpaywall remains part of the identifier source policy. Live vault configs should keep Unpaywall provider email in runtime env files for DOI-backed OA fallback, not in the first-pass source list.
- Paper Source 2.3.2: add optional `grok-search-rs MCP` supplemental discovery to the Paper Source discovery workflow. Vault config now exposes `grok_search.mode` (`targeted`, `parallel`, `off`), targeted/parallel query budgets, Grok-only recommendation cap, and academic gap domains; runtime config adds a sanitized `grok_search_mcp` command/env-file section. Default targeted mode runs paper-search first and skips Grok when paper-search is good enough; parallel mode starts static shortfall-oriented Grok queries alongside the full paper-search lifecycle without shortening paper-search. Provider artifacts are separated (`paper-search-record.json`, `grok-search-record.json`, `grok-search-raw.json`, `grok-search-evidence.json`) before merged `search-record.json`; duplicate merge is paper-search-priority while Grok can add PDF/landing/evidence fields. Runtime config now treats tool wiring as first-class plugin configuration: Grok-compatible public env keys are accepted from env files, `runtime_config.py` emits a `path_policy`, and `doctor --json` reports `runtime_path_policy` warnings when installed-user runtime depends on development checkouts, project `.env` helper scripts, vault internals, temp paths, or plugin cache paths. Final recommendations still require a usable paper-search candidate; Grok-only salvage is evidence/diagnostics only. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.3.1: add configurable MinerU CDN ZIP recovery for fake-IP/TLS environments. `PAPER_SOURCE_MINERU_CDN_RESOLVE=cdn-mineru.openxlab.org.cn=<ip>` lets the MinerU batch downloader retry `full_zip_url` with a temporary host/IP DNS override while preserving the original HTTPS hostname/SNI, records `download_recovery` metadata in `mineru-manifest.json`, and keeps normal DNS behavior unchanged when unset. The `mineru-paper-parser` skill entry stays thin and points detailed troubleshooting to `references/mineru_api.md`. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.3.0: add explicit Codex automation approval handoff on the existing `human-approval.json` / `paper-source-human-approval-v1` contract. `record-human-approval` now accepts opt-in automation fields for `codex-automation:<task-id>` actors, validates actor/task consistency, records auditable task/source/original-authorization metadata, and passes automation context through `wiki-agent-trigger.json` without making ordinary `discover-papers` or auto-staging infer automation. Paper Source still stops at approval/trigger/record artifacts; Paper Wiki or the current wiki-capable agent still owns formal page writing. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.2.1: align `paper-discovery/agents/openai.yaml` with the `discover-papers` natural-language default so UI-facing skill metadata no longer describes only generic search/rank behavior. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.2.0: add the high-level `discover-papers` command for natural-language discovery. It composes existing `dry-run` evidence generation with `paper_source.auto_staging`, keeps review/survey/meta-analysis papers included by default unless explicit non-review intent is present, records `discover-papers-record.json`, links discovery and auto-staging artifacts, and stops before approval, wiki-ingest trigger/record, and Paper Wiki formal pages. `dry-run` remains the lower-level evidence/debug command. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.1.1: add `auto_staging.py` and `paper-source-auto-staging-plan-v1` for automatic staging policy over `report.json.session_recommendations`. The policy selects at most 3 PDF-available primary recommendations, caps review/survey/meta-analysis papers to 1 default slot unless explicitly requested, skips `needs_pdf` papers while preserving manual-download links, and records `auto_staging_plan` plus refreshed `auto_staging_status` without creating approval, Paper Wiki trigger, final page, or record artifacts. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.1.0: add `report.json.session_recommendations` / `paper-source-session-recommendations-v1` as the chat-facing recommendation contract. Primary recommendations are capped, exclude `review-candidate` papers, preserve Tier C/review items in a compact appendix, summarize rejected reasons, and leave semantic Chinese summaries to the calling agent from original abstract evidence. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 2.0.0 (slim-down, breaking): remove dead/deprecated code. Delete the standalone `migrate_active_internal_artifacts.py` migration script, the deprecated `promote_to_wiki.py` compiled-draft path, and the legacy `epi_repository.py` import shim, together with the user-visible `promote-to-wiki` and `rollback-promotion` CLI commands. The agent-mediated `wiki-ingest-handoff` + `record-human-approval` + `wiki-ingest-trigger` + `record-wiki-ingest` flow remains the only golden path. `paper-source-repository-migrate` / `paper-source-repository-cleanup` are unaffected. Source-checkout change only; marketplace refresh / reinstall / installed-cache verification remain to be run separately.
- Paper Source 1.1.0: add transparent hard/soft query-plan provenance, `discovery-diagnostics.json`, configurable `--selection-policy`, missing-PDF readiness separation, wiki `_meta/reference-index.json` first dedupe with `already_in_wiki:<page>`, and `discover-to-handoff` as an explicit discovery -> source-staging wrapper that does not approve papers or write final wiki pages.
- Paper Source 1.0.6: switch the `paper-search-mcp` outer MCP registration to `cmd /c .\scripts\paper_search_mcp_launcher.cmd` with plugin-root `cwd: "."`, so Codex Desktop does not depend on its own `python` PATH before the launcher can load runtime.json and select the configured Python interpreter.
- Paper Source 1.0.5: fix Codex MCP self-registration for `paper-search-mcp` by using plugin-root `cwd: "."` plus `./scripts/paper_search_mcp_launcher.py` instead of an unresolved `${CLAUDE_PLUGIN_ROOT}` path, and teach `doctor --json` to flag unresolved plugin-root placeholders before they become opaque handshake failures.
- Paper Source 1.0.4: harden MinerU output ZIP retrieval. MinerU batch downloads now retry transient CDN/network failures, write `download_failed` manifest entries, and surface concrete output-download errors in `parse-record.json` instead of only reporting missing Markdown.
- Paper Source 1.0.3: add structured agent query plans and request constraints for discovery. `dry-run` accepts `--agent-query-plan-json`, `--year-min`, and `--code-policy ignore|prefer|require`; run artifacts record `request_constraints`, filter enforces recency/code requirements, and ranking/report output surfaces code and constraint evidence.
- Paper Source 1.0.2: remove built-in discipline query packs from paper discovery. The query planner and `dry-run --query-plan-domain` now accept only `auto` / `profile`; field vocabulary must come from config, Research Briefs, current user requests, or explicit agent-supplied query variants/domain focus terms.
- Paper Source 1.0.1: add agent-supplied discovery inputs. `dry-run` accepts repeated `--query-variant` and `--domain-focus-term`, records them in `query-plan.json`, and executes compiled academic queries instead of using raw natural-language topics as MCP search strings. Also tightens excluded-term matching so `surveying` is not rejected as `survey`.
- Paper Source 1.0.0 / Paper Wiki 1.0.0: current-only handoff and naming cleanup. `wiki-ingest-brief.json` is the only new Paper Source-to-Paper Wiki handoff; old aliases are not user entrypoints, route triggers, env fallbacks, or new artifact contracts.
- Paper Source 0.2.6: historical installed-cache/source-line marker retained for upgrade audits; current runtime claims must still be checked against the installed plugin cache.
- Paper Source 0.2.5 / Paper Wiki 0.2.4: enforce title-display canonical PDF Markdown links in formal-page `sources` and body source PDF entries, reject `原论文 PDF` as clickable link text, refresh active `_paper_source` sidecars, and document the installed-cache refresh boundary.
- S3b brief-first machine-contract 收敛用户黄金路径：`wiki-ingest-brief.json` 作为新链路 canonical handoff，`wiki_deposition_task.json` 只作为历史 handoff 清理对象，旧别名不再作为用户入口或路由触发条件。

## Paper Source 0.2.4 / Paper Wiki 0.2.3 (2026-06-11)

- Paper Source/Paper Wiki 合同统一为新正式页 `lifecycle: draft`，不再新增 `review_status`；旧 `lifecycle: review-needed` 只作为 legacy repair input。
- Frontmatter `sources` use title-display canonical `_paper_source/raw/<slug>/paper.pdf` Markdown PDF links; body `## 原文与证据入口` uses the same PDF URI with the paper title as clickable text. Formal wikilinks still must not point to `_paper_source/` or legacy `_epi/`.
- 公式和符号证据顺序改为 MinerU Markdown 主源；只有 Markdown 缺失、错误、歧义或不足时回退 PDF、figure/formula indexes 和图片证据，非空原生 `mineru/paper.tex` 仅作可选交叉复核。
- Paper Wiki 合同扩展为 formal page、正式内容关系、stale claim、split/merge、图谱/索引/追踪/provenance/readiness 维护层。

## 0.2.3 (2026-06-10)

- Paper Source skill trigger descriptions 再精炼：保留中文任务触发词、Paper Source/Paper Wiki 边界和 research-grill-me deep-research prompt 契约，降低默认触发上下文成本。

## 0.2.2 (2026-06-10)

- Paper Source / Paper Wiki skill 入口压缩：保留路由、来源边界、handoff gate 和 post-task check，减少默认加载成本。
- Paper Source / Paper Wiki manifest 和默认 prompt 精简为短入口，版本同步到 `0.2.2`。
- Paper Wiki 继续保持一个用户级 `$paper-research-wiki` 助手，`paper-wiki-language` 作为正式页语言 gate。

## 0.2.1 (2026-06-07)

- S3a 文档/契约 canonical 化：A1 overview 导航化。
- A2 workflow 巨段拆成短入口。
- A3 doc map 收敛到 `docs/paper-source-linkage.md`。
- A4 progress 快照化，历史迁入本文件。
- A5 page-family/frontmatter 人读 canonical 指向 Paper Wiki `rules/wiki-writing-standard.md`。
- C3 只读问答归属：Paper Wiki `ask_wiki` 是对话主入口，Paper Source `wiki-ask` 是同源 fallback / 程序化入口。
- C4 `wiki-ingest-brief.json` 是 canonical handoff；`wiki_deposition_task.json` 是 deprecated 兼容 artifact。
- D3 删除 `_write_json` 私有别名，直接调用 `write_json_atomic`。
- E1 Paper Source plugin description 改为 discovery -> MinerU -> critic -> wiki handoff 全链路定位。

## 0.2.0 (2026-06-07)

- 增加 read-only formal graph `wiki-ask`。
- Paper Wiki ask-mode record automation 写 `paper-wiki-record-request.json`。
- Paper Source `record-wiki-ingest --from-paper-wiki-request` 消费 Paper Wiki request artifact；旧 record request artifact 只属于历史迁移语境，当前用户入口不再使用旧别名。
- Source manifest 同步到 Paper Source `0.2.0` / Paper Wiki `0.2.0`。

## 0.1.14 / 0.1.7

- Paper Wiki -> Paper Source ask-mode record automation。
- `automation_mode=ask` 与 live page hash 验证。
- `final-source-review.json` 与 human approval identity 一起进入 record gate。

## 0.1.13 / 0.1.6

- Workflow boundary source version sync。
- 明确 Paper Wiki `$paper-research-wiki` 与 Paper Source `paper-source-paper-deposition` 历史 handoff 清理边界。
- 更新 formal page gate 与 record provenance 说明。

## Earlier 0.1.x Notes

- Runtime 配置进入用户级 `runtime.json`。
- `paper-search` MCP/CLI fallback、provider readiness 和 MinerU env file 分层。
- `prepare-ranked --skip-existing` 防止补跑时已解析论文占用预算。
- `paper-quality-critic`、`parse-quality-critic` 和 role critic quorum 进入 audited ingest。
- `run-lifecycle` 与 bounded lifecycle cleanup 加入默认维护边界。
- Plugin Eval 历史信号包含 pass rate `1` 和 `82/100`；这些是历史快照，发布前必须重跑。
